apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: payment-server
  namespace: centralized-auth
spec:
  http:
    - name: payment-server-http
      match:
        hosts:
          - centralized-auth.local
        paths:
          - "/payment-server/*"
      backends:
        - serviceName: payment-server
          servicePort: 3000
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri: ["^/payment-server/(.*)", "/$1"]
#        - name: jwt-auth
#          enable: true
#          config:
#            public_key: |
#              -----BEGIN PUBLIC KEY-----
#              ...
#              -----END PUBLIC KEY-----
#            algorithm: RS256
#            header:
#              "X-User-Name": "$username"
